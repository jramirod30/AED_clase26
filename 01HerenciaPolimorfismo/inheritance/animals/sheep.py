class Sheep:

    # Debería ser, estático ya que no usa self, es decir
    # debería ser de clase. Pero estamos en un ejemplo que vamos
    # a ir retocando
    def play_sound(self) -> None:
        print("beee!!")

    # Debería ser, estático ya que no usa self, es decir
    # debería ser de clase. Pero estamos en un ejemplo que vamos
    # a ir retocando
    def graze(self) -> None:
        print("grazing grass")
